#version 460
// Rotary position embedding over [T, H, dh] activations, output
// mirroring the input layout (see op_rope_body.glsl).

#extension GL_GOOGLE_include_directive : require

#include "op_rope_body.glsl"
